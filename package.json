{
  "name": "homepage-hostname-proxy",
  "version": "1.0.0",
  "description": "Transparent proxy that rewrites NAS hostnames in Homepage dashboard responses",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  }
}